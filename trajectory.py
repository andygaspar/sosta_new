import pandas as pd

# df = pd.read_csv("Coppie_OD/BIKF_EBBR.csv", sep="\t")
# for tb in df["trajectory"]:
#     t = wkb.loads(tb, hex=True)
#     numerics = np.array(t)
#     # if checkOD(numerics, origin, destination):
#     t = LineString(numerics * np.array([1.0, 1.0, 0.0007]))
#     dataset.append(t)
#     # else:
#     d2.drop(tb.Index, inplace=True)
# airport.to_csv("data/airports.csv", index=False)
# dataset = []
#

